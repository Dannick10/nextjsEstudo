import React from "react";
import Layout from "@/components/template/Layout";

type Props = {};

const notificacoes = (props: Props) => {
  return (
    <Layout titulo="Notificações" subtitulo="Aqui voçê ira gerenciar as suas notificações">
      <h3>Conteudo</h3>
    </Layout>
  );
};

export default notificacoes;
